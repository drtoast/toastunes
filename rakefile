task :export_artwork, :destination do |t,args|
  require './lib/ttunes.rb'
  args.with_defaults(:destination => File.join(File.dirname(__FILE__), 'public','images'))
  TTunes.export_artwork(args.destination)
end
